<%= form_for(@uname) do |f| %>
  <% if @uname.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uname.errors.count, "error") %> prohibited this uname from being saved:</h2>

      <ul>
      <% @uname.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :named_class %><br />
    <%= f.text_field :named_class %>
  </div>
  <div class="field">
    <%= f.label : %><br />
    <%= f.text_field : %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
